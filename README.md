# Aprender Inglês - Salesforce

Uma aplicação React simples para treinar vocabulário e frases comuns usadas em projetos Salesforce.

## Visão geral
- Interface minimalista para estudar termos (vocabulário) e frases de trabalho.
- Exibe uma palavra e uma frase aleatória; permite navegar por lições pré-definidas.

## Funcionalidades
- Lista de vocabulário básico de Salesforce.
- Lista de frases comuns no ambiente de trabalho.
- Modo aleatório para mostrar uma palavra e uma frase.
- Navegação simples entre menu e lições.

## Tech stack
- React (Create React App)
- JavaScript, CSS

## Instalação
1. Clone o repositório:

```bash
git clone <URL-do-repo>
cd ingles-salesforce
```

2. Instale dependências:

```bash
npm install
```

3. Execute em modo desenvolvimento:

```bash
npm start
```

## Scripts úteis
- `npm start` — inicia a aplicação em modo de desenvolvimento.
- `npm run build` — gera a build de produção.
- `npm test` — executa testes (se houver).

## Estrutura principal
- `src/App.js` — componente principal com o conteúdo e lógica.
- `public/index.html` — página HTML principal.
- `package.json` — scripts e dependências.

## Como usar
- Abra o app no navegador (normalmente `http://localhost:3000`).
- Clique em "Vocabulário e Frase Aleatória" para ver itens aleatórios.
- Clique em uma lição para ver o vocabulário/frases da lição.

## Contribuição
Contribuições são bem-vindas — abra issues ou pull requests com melhorias.

## Licença
Licença MIT (ou ajuste conforme necessário).

## Contato
Se precisar de ajuda, abra uma issue ou entre em contato com o mantenedor do repositório.
